apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "distributed-calculator.fullname" . }}-test-connection"
  labels:
    {{- include "distributed-calculator.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "distributed-calculator.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
