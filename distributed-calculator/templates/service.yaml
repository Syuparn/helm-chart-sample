apiVersion: v1
kind: Service
metadata:
  name: {{ include "distributed-calculator.fullname" . }}
  labels:
    {{- include "distributed-calculator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "distributed-calculator.selectorLabels" . | nindent 4 }}
